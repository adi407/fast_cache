<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.adi407</groupId>
  <artifactId>fastcache-parent</artifactId>
  <version>1.1.0</version>
  <packaging>pom</packaging>
  <name>FastCache</name>
  <description>Zero-infrastructure, off-heap, virtual-threaded cache engine for AI workloads</description>

  <url child.project.url.inherit.append.path="false">https://github.com/adi407/fast_cache</url>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>adi407</id>
      <name>Aditya Kumar</name>
      <url>https://github.com/adi407</url>
    </developer>
  </developers>

  <scm child.scm.connection.inherit.append.path="false"
       child.scm.developerConnection.inherit.append.path="false"
       child.scm.url.inherit.append.path="false">
    <connection>scm:git:https://github.com/adi407/fast_cache.git</connection>
    <developerConnection>scm:git:git@github.com:adi407/fast_cache.git</developerConnection>
    <url child.project.url.inherit.append.path="false">https://github.com/adi407/fast_cache</url>
    <tag>HEAD</tag>
  </scm>

  <modules>
    <module>fastcache-engine</module>
    <module>fastcache-spring-boot-starter</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring.boot.version>3.3.4</spring.boot.version>

    <!--
      Without these, Maven appends each module's artifactId to the inherited <url> and <scm> values,
      publishing dead links like .../fast_cache/fastcache-engine on every child artifact.
    -->
    <maven.compiler.parameters>true</maven.compiler.parameters>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.github.adi407</groupId>
        <artifactId>fastcache-engine</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <release>${java.version}</release>
            <compilerArgs>
              <arg>-Xlint:all,-serial,-processing</arg>
              <arg>-parameters</arg>
            </compilerArgs>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.2.5</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.4.1</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <profiles>
    <!--
      Everything Maven Central requires, kept out of the default build so day-to-day `mvn verify` stays
      fast and needs no GPG key. Activated by `mvn -Prelease deploy`, which is what the release workflow
      runs on a version tag.

      Central rejects a deployment that is missing any of: a -sources.jar, a -javadoc.jar, a .asc
      signature for every artifact, or the name/description/url/licence/developer/scm metadata (which
      lives in this pom and is inherited by both modules).
    -->
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <version>3.3.1</version>
            <executions>
              <execution>
                <id>attach-sources</id>
                <goals><goal>jar-no-fork</goal></goals>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>3.6.3</version>
            <configuration>
              <!--
                Records document their components in the class comment rather than with @param tags, so
                strict doclint reports a warning per component. Everything else stays checked; this
                suppresses only the missing-tag category.
              -->
              <doclint>all,-missing</doclint>
              <quiet>true</quiet>
            </configuration>
            <executions>
              <execution>
                <id>attach-javadocs</id>
                <goals><goal>jar</goal></goals>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>3.2.4</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals><goal>sign</goal></goals>
                <configuration>
                  <!--
                    loopback pinentry is required on a CI runner: there is no TTY for gpg to prompt on,
                    and without it the signing step hangs until the job times out.
                  -->
                  <gpgArguments>
                    <arg>--pinentry-mode</arg>
                    <arg>loopback</arg>
                  </gpgArguments>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <!--
              0.7.0 uploaded the bundle fine and then crashed polling for validation status: the Portal
              API had grown a "warnings" field that the plugin's JSON parser rejected as unknown. The
              artifacts were never the problem - the client was simply older than the server. Pinning a
              current version keeps the two in step.
            -->
            <version>0.11.0</version>
            <extensions>true</extensions>
            <configuration>
              <!-- Matches the <server><id> the workflow writes into settings.xml. -->
              <publishingServerId>central</publishingServerId>
              <!--
                Deliberately false: the deployment lands in the Portal as a validated draft and a human
                clicks publish. A release to Central is irreversible - versions can never be deleted or
                overwritten - so the first few releases should not be one tag push away from permanent.
              -->
              <autoPublish>false</autoPublish>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
